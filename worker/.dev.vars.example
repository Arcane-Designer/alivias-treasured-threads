# Copy to .dev.vars for local Stripe test mode. Never commit the real file.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Approved flat standard shipping per US order: $6.00.
SHIPPING_RATE_CENTS=600
SHIPPING_MIN_DAYS=
SHIPPING_MAX_DAYS=
PAYMENTS_MODE=
